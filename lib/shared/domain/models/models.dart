// Barrel intentionally empty: the shared layer must NOT re-export feature-layer
// models. Cross-cutting models (e.g. `User`) live directly in this folder and
// are imported by their own path, e.g.
// `package:flutter_clean_arch_unicorn/shared/domain/models/user.dart`.
