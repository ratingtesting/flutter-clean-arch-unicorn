// Barrel intentionally empty: the shared layer must NOT re-export feature-layer
// models. Import feature models directly, e.g.
// `package:flutter_clean_arch_unicorn/features/authentication/domain/models/user_model.dart`.
